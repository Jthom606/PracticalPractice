#include <stdio.h>
int main(void)
{
    char name[25]; //character limit
    printf("Hello. What's your name?\n"); //question
    fgets(name, 20, stdin); // user input
    printf("Hi there, %s", name); //output of user input
    return 0;

}
